name: First_flow
on: [push]

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display some text
        run: echo "Some text"
      - name: Command in multiple lines
        run: |
            node -v
            npm -v